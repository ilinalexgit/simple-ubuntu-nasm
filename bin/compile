#!/bin/sh

build_dir=$1
cache_dir=$2
env_dir=$3

vendor_dir="$build_dir/vendor"

echo "-----> Installing NASM $VERSION"

apt-get update
apt-get install nasm

echo "-----> TEST $PATH"
echo "-----> TEST $cache_dir"
echo "-----> TEST $build_dir"
nasm -v
echo "-----> TEST END"

exit 0
